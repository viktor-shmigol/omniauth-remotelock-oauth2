# frozen_string_literal: true

RSpec.describe Omniauth::Remotelock::Oauth2 do
  it "has a version number" do
    expect(Omniauth::Remotelock::Oauth2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
